import { pageElements } from "./dom/page-elements";

window.addEventListener("load", () => {
  const {
    diagramDiv,
    paletteDiv,
    addGirlButton,
    addBoyButton,
    saveButton,
    loadButton,
    exportToPngButton,
    exportToSvgButton,
    toggleGroupsButton,
    undoButton,
    redoButton,
  } = pageElements;
});
